import XCTest
@testable import swiftui_pick_better

final class swiftui_pick_betterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swiftui_pick_better().text, "Hello, World!")
    }
}
